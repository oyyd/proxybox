import React from 'react'
import ReactDOM from 'react-dom'
// NOTE: the "__dirname" here is as the same as assets/app.html
import App from '../lib/component/app'

ReactDOM.render(<App />, document.getElementById('main'))
